<?xml version="1.0" encoding="utf-8"?>
<!--
  Sparse / external-location package manifest (phase 4 · B1b) — gives the
  unpackaged SwiftCrossUI app a package IDENTITY so swift-winui skips the
  Windows App Runtime bootstrapper (Initialize.swift:103-105) and resolves the
  already-installed WindowsAppRuntime 1.5 framework via the static
  <PackageDependency> below — no DDLM 1.5 required. See HANDOVER §8.

  This manifest is registered loose (Developer Mode) next to the built .exe via
  Register-StackConnectApp.ps1; the .exe keeps running from the SwiftPM build dir.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap10="http://schemas.microsoft.com/appx/manifest/uap/windows10/10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">

  <Identity Name="StackConnect.WindowsApp"
            Publisher="CN=StackConnectDev"
            Version="0.1.0.0"
            ProcessorArchitecture="arm64" />

  <Properties>
    <DisplayName>StackConnect</DisplayName>
    <PublisherDisplayName>StackConnect</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.22621.0" />
    <PackageDependency Name="Microsoft.WindowsAppRuntime.1.5"
                       MinVersion="5001.373.1736.0"
                       Publisher="CN=Microsoft Corporation, O=Microsoft Corporation, L=Redmond, S=Washington, C=US" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="StackConnectWindowsApp"
                 Executable="StackConnectWindowsApp.exe"
                 EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="StackConnect"
        Description="StackConnect Windows port (SwiftCrossUI / WinUI)"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png" />
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
